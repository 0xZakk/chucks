<%=link_to "Trending", quotes_path%>
<%= form_tag new_response_path, method: 'get' do %>
  <%= select_tag 'category_filter', options_for_select(Quote.quote_category.uniq)%>
   <%= submit_tag "Send", id: "play_button"%>
<% end%>
